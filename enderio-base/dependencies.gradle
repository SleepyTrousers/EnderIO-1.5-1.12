modDependencies {
	forge { requireAfter(forge_version) }
	endercore { requireAfter(endercore_version) }
	jei { loadAfter(jei_version) }
	buildcraft { loadAfter(buildcraft_version) }
	baubles { loadAfter(baubles_version) }
	thaumcraft { loadAfter(thaumcraft_version)}
	tickcentral { loadAfter("3.1")} // there's a mod incompatibility with 2.x https://github.com/TerminatorNL/TickCentral/issues/13
}
